enum AttendanceEnum {
  PRESENTE
  AUSENTE
}

model AttendanceSession {
  id              Int               @id @default(autoincrement())
  eventInstanceId Int
  modality        String
  checkIn         DateTime
  checkOut        DateTime?
  EventInstance   EventInstance     @relation(fields: [eventInstanceId], references: [id])
  createdAt       DateTime          @default(now())
  createdBy       Int
  delegateId      Int
  Delegate        Person            @relation(fields: [delegateId], references: [id])
  EventAttendance EventAttendance[]
}

model EventAttendance {
  id                   Int               @id @default(autoincrement())
  attendanceSessionId  Int
  personId             Int
  checkIn              DateTime
  checkOut             DateTime?
  status               AttendanceEnum
  comment              String?
  justificationFileUrl String?
  coordinates          String?
  Person               Person            @relation(fields: [personId], references: [id])
  AttendanceSession    AttendanceSession @relation(fields: [attendanceSessionId], references: [id])
  createdAt            DateTime          @default(now())
  updatedAt            DateTime          @updatedAt
  createdBy            Int
  updatedBy            Int?

  @@unique([personId, attendanceSessionId])
}

model WorkAttendance {
  id                Int            @id @default(autoincrement())
  personId          Int // DE QUIÉN es la asistencia (obligatorio)
  date              DateTime       @db.Date
  checkIn           DateTime
  checkOut          DateTime?
  status            AttendanceEnum @default(PRESENTE)
  comment           String?
  justificationFile String?
  coordinates       String?

  Person Person @relation(fields: [personId], references: [id])

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  updatedBy Int? // solo para correcciones posteriores

  @@unique([personId, date])
  @@index([date])
  @@index([personId])
}
